node_modules
dist

# Generated by drizzle-kit and read back by it. Reformatting these would be
# pure churn at best; `meta/_journal.json` in particular must never be touched
# by anything but the generator — the migrator applies only migrations newer
# than the last applied timestamp, so a rewritten entry can silently skip a
# migration at boot.
drizzle/

# Vendored third-party oxlint plugin — kept byte-identical to upstream.
tools/oxlint/anti-slop

# Mail template fixtures in the legal file contract's markdown subset
# (M246/04). Prettier would rewrite `*emphasis*` as `_emphasis_`, which the
# contract refuses, and the refused fixture must stay refused.
tests/fixtures/content
tests/fixtures/content-refused
